# Alankruthi Frontend

React + Vite frontend for the Alankruthi custom embroidery ecommerce platform.

## Scripts

```bash
npm run dev
npm run build
npm run lint
npm run preview
```

Set `VITE_API_URL` in `.env` to point at the backend API. See `.env.example`.
